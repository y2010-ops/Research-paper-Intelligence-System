# Research Paper Intelligence System

A system for analyzing and querying research papers using RAG and Knowledge Graphs.

## Setup
1. Run `setup.ps1` (Windows) or `setup.sh` (Linux/Mac)
2. Edit `.env` with your API keys.
3. Run `python backend/main.py`
